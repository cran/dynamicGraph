#
#

cd /home/jhb/CoCo/Rsource/CoCo/dynamicGraph
mkdir Affald
mkdir Affald/old
mv R man Affald/old

cd inst
R --no-save < Rhistory 
mv *.Rd dynamicGraph/man/
cd ..

# mv inst/dynamicGraph/data .
mv inst/dynamicGraph/R .
mv inst/dynamicGraph/man .

ls -laR inst/dynamicGraph

rm -r inst/dynamicGraph

# cp -p ~/DynamicGraph/dynamicGraph/src/First.R R/DotFiles.R

cd Work
ls -la ../man > File.List

# Change ./Work/change.decribe !!!

rm *~

cd ..
./Work/change.decribe >& Work/change.decribe.log

cat Work/change.decribe.log 

# % \usage{ } in ../man/redrawGraphWindow.Rd
# Move/Delete code from ../man/dynamicGraphMain.Rd
# incomplete final line found by readLines on `man/dynamicGraph-internal.Rd'

mv man man.orig.22
mkdir man
cp -p /home/jhb/DynamicGraph/dynamicGraph/MAN/*.Rd man

cd ..
R CMD check dynamicGraph > check.dynamicGraph.1.log
